@using FoodAdvisor.ViewModels.PlaceViewModels;
@model PlaceDetailsViewModel

@{
	ViewBag.Title = "Place Details";
}

<h2>@Model.Name</h2>
<div .container-fluid>
	<div class="row">
		<img src="@Model.ImageURL" class="img-fluid" alt="Responsive image">
		<p><strong>Name</strong> @Model.Name</p>
		<p><strong>Category</strong> @Model.Category</p>
		<p><strong>Description</strong> @Model.Description</p>
		<p><strong>Address:</strong> @Model.Address</p>
		<p><strong>City:</strong> @Model.City</p>
		<p><strong>Publisher:</strong> @Model.Publisher</p>
	</div>
</div>

<a asp-action="Index" class="btn btn-secondary">Back to All Movies</a>